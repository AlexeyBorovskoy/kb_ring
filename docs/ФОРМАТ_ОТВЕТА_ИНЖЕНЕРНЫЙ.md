# Формат инженерного ответа (шаблон)

Источник: `/home/alexey/shared_vm/ответ по коду в gitlab.txt`.

Цель: ответы KB-RING должны быть не “общими”, а инженерными: с конкретными интерфейсами, примерами, путями и источниками.

## Шаблон ответа

### Запрос
`<текст запроса>`

### Резюме
Коротко: что найдено, сколько источников, где именно (GitLab/Nextcloud и т.п.).

### <Раздел 1: например "Интерфейсы">

Таблица (если подходит):

| Тип | Назначение | Источник |
|-----|------------|----------|
| REST API | ... | GitLab/... |
| TCP протокол | ... | Nextcloud/... |

Далее подробности по каждому пункту:

1. `<Название>`
   - Назначение: ...
   - Методы/контракты: ...
   - Пример:
     ```bash
     curl ...
     ```
   - Источник: `...` (и ссылка `[1]`)

2. `<Название>` ...

### Итог
- Основные выводы: ...
- Рекомендуется начать изучение с:
  1. ...
  2. ...

### Источники (обязательно)
[1] `<uri>`
[2] `<uri>`

## Правила

- Нельзя придумывать факты.
- Если данных недостаточно: прямо написать `"данные не найдены"`.
- Источники обязательны для RAG-ответов.
- Внутри текста по возможности ссылаться на источники как `[1]`, `[2]`, ...

